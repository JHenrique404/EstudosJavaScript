const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) //constante dentro de Math
                       /* Primeiro paratro raio
                        * 2 e elevado ao quadrado
                        * pow e de power de potencia
                       */

console.log(area.toFixed(2)) 
console.log(typeof Math)